package packages;
// This is package declaration for the Demo class, which is part of the 'packages' package.
// The Demo class demonstrates the usage of the E class from the same package.
// In this we will learn about packages in java. 
// Packages are used to group related classes and interfaces together. 
// It helps in avoiding name conflicts and also provides access control. 
// There are two types of packages in java: built-in packages and user-defined packages. 
// Built-in packages are those that come with the Java Development Kit (JDK) and,
// User-defined packages are those that we create ourselves.
// Any file that exist in the package directory is part of the package and,
// Can't be accessed outside the package unless it is declared as public.\
// For the file to be accessed outside the package, it must be declared as public and the file name must match the class name.
// Variable should not be declared as public but the method should be public to be accesessed outside the package.
public class Demo {
    public static void main(String [] args) {

    }
}
